name: Build reF1nd
on:
  workflow_dispatch:
  schedule:
    - cron: "00 23 * * *"
  push:
    branches:
      - master
      - hidden
    paths-ignore:
      - "**/README.md"
      - "**/template.json"

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 3
          keep_minimum_runs: 6

  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - { os: freebsd, arch: riscv64 }
          - { os: freebsd, arch: amd64 }
          - { os: freebsd, arch: 386 }
          - { os: freebsd, arch: arm64 }
          - { os: openbsd, arch: riscv64 }
          - { os: openbsd, arch: amd64 }
          - { os: openbsd, arch: 386 }
          - { os: openbsd, arch: arm64 }
    outputs:
      new: ${{ steps.check.outputs.new }}
      version: ${{ steps.check.outputs.version }}
    steps:
      - name: Set variables
        run: |
          echo "tags=with_gvisor,with_dhcp,with_wireguard,with_utls,with_acme,with_clash_api,with_tailscale,with_quic" >> $GITHUB_ENV
        shell: bash

      - name: Checkout the "hidden" branch
        uses: actions/checkout@v4
        with:
          ref: hidden

      - name: Apply patches
        run: |
          git clone --depth 1 -b main https://github.com/Sagernet/sing-tun.git sing-tun
          git clone --depth 1 https://github.com/metacubex/tfo-go.git tfo-go
          git clone --depth 1 -b reF1nd-main https://github.com/reF1nd/sing-box.git sing-box
          ls -lha
          cd sing-tun
          patch -p1 < ../sing-tun.patch
          cp monitor_darwin.go monitor_freebsd.go
          cp monitor_darwin.go monitor_openbsd.go
          cd ../sing-box
          patch -p1 < ../sing-box-reF1nd.patch
          sing_tun_version=$(cat go.mod | grep github.com/sagernet/sing-tun | awk '{ print $2 }')
          echo -e "\nreplace github.com/sagernet/sing-tun ${sing_tun_version} => ../sing-tun" >> go.mod
          tfo_go_version=$(cat go.mod | grep github.com/metacubex/tfo-go | awk '{ print $2 }')
          echo -e "\nreplace github.com/metacubex/tfo-go ${tfo_go_version} => ../tfo-go" >> go.mod
          latest_version=$(cat docs/changelog.md | grep '####' | head -n 1 | sed 's|#||g;s| ||g')
          echo "RELEASE_NAME=${latest_version}-vincent" >> $GITHUB_ENV
          echo "TAG_NAME=v${latest_version}-vincent" >> $GITHUB_ENV
          git_head=$(git rev-parse --short HEAD)
          echo "version=${git_head}" >> $GITHUB_ENV

      - name: Check for new version
        id: check
        run: |
          if [ -f last_version ]; then
            LAST_VERSION=$(cat last_reF1nd_version)
          else
            LAST_VERSION="none"
          fi
          echo "Last version: ${LAST_VERSION}, current version: ${{ env.version }}"
          if [ "${{ env.version }}" != "${LAST_VERSION}" ]; then
            echo "${{ env.version }}" > last_reF1nd_version
            echo "new=true" >> $GITHUB_ENV
            echo "new=true" >> $GITHUB_OUTPUT
          else
            echo "new=false" >> $GITHUB_ENV
            echo "new=false" >> $GITHUB_OUTPUT
          fi
          echo "version=${version}" >> $GITHUB_OUTPUT

      - name: Setup Go
        if: github.event_name != 'schedule' || env.new == 'true'
        uses: actions/setup-go@v5
        with:
          go-version: "stable"

      - name: Cache go dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Build binary file
        if: github.event_name != 'schedule' || env.new == 'true'
        run: |
          cd sing-box
          go build -v -trimpath -ldflags "-X 'github.com/sagernet/sing-box/constant.Version=${RELEASE_NAME}' -s -w -buildid=" -tags ${tags} ./cmd/sing-box
          mv sing-box sing-box-reF1nd-${{ matrix.os }}-${{ matrix.arch }}
        env:
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 0

      - name: Compress and move file to publish directory
        if: github.event_name != 'schedule' || env.new == 'true'
        run: |
          cd sing-box
          xz -v sing-box-reF1nd-${{ matrix.os }}-${{ matrix.arch }}
          mkdir -p ../publish
          cp -v sing-box-* ../publish/

      - name: Release and upload assets
        if: github.event_name == 'schedule' && env.new == 'true'
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ env.RELEASE_NAME }}
          tag_name: ${{ env.TAG_NAME }}
          draft: false
          prerelease: false
          files: |
            ./publish/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  push:
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' && needs.build.outputs.new == 'true'
    needs:
      - build
    steps:
      - name: Checkout the "hidden" branch
        uses: actions/checkout@v6
        with:
          ref: hidden

      - name: Update last_reF1nd_version
        run: |
          echo "${{ needs.build.outputs.version }}" > last_reF1nd_version

      - name: Push last_reF1nd_version to "hidden" branch
        run: |
          git config --local user.name "Vincent Loeng"
          git config --local user.email "173226116+Vincent-Loeng@users.noreply.github.com"
          git add last_reF1nd_version
          git commit -m "Release ${{ needs.build.outputs.version }} (reF1nd)"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
